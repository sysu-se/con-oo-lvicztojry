import { writable } from 'svelte/store';
import { gameStore } from '@sudoku/gameStore';

export const gamePaused = writable(true);

// gameWon 现在从 gameStore.isComplete 派生
// 当领域对象标记游戏完成时，gameWon 变为 true
export const gameWon = gameStore.isComplete;